---
args:
  positional:
    - name: event
      description: The field that holds the event to map.
      type: field
    - name: ocsf
      description: The OCSF output field.
      type: field
---
// --- OCSF: classification attributes ----------

let $activity_id = {
  discover: 1,
  offer: 2,
  request: 3,
  decline: 4,
  ack: 5,
  nak: 6,
  release: 7,
  inform: 8,
}

$ocsf.class_uid = 4004
$ocsf.activity_id = $activity_id[$event.dhcp.dhcp_type.string()] else 0
$ocsf.severity_id = 1
$ocsf.type_uid = $ocsf.class_uid * 100 + $ocsf.activity_id

// --- OCSF: primary attributes -----------------

if $event.dhcp.type? == "reply" {
  $event._server_endpoint = $ocsf.src_endpoint
  $ocsf.src_endpoint = $ocsf.dst_endpoint
  $ocsf.dst_endpoint = $event._server_endpoint
  drop $event._server_endpoint
}

if $event.dhcp.has("hostname") {
  $ocsf.src_endpoint.hostname = move $event.dhcp.hostname
}

if $event.dhcp.has("client_mac") {
  $ocsf.src_endpoint.mac = move $event.dhcp.client_mac
}

if $event.dhcp.has("assigned_ip") {
  $ocsf.src_endpoint.ip = move $event.dhcp.assigned_ip
}

if $event.dhcp.has("lease_time") {
  $ocsf.lease_dur = move $event.dhcp.lease_time
}

match $event.dhcp.dhcp_type {
  "offer" | "ack" => {
    $ocsf.status_id = 1
  }
  "decline" | "nak" => {
    $ocsf.status_id = 2
  }
  _ => {
    $ocsf.status_id = 0
  }
}

$ocsf.transaction_uid = (move $event.dhcp.id).string()

// --- Finalize ---------------------------------

@name = "ocsf.dhcp_activity"
