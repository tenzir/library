---
description: Normalizes alphaMountain category feed fields.
---

if categories? != null {
  category_ids = move categories
  category_labels = category_ids.map(id => f"alphaMountain category {id}")
}
