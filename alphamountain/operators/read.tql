---
description: Parses alphaMountain CSV API responses.
---

read_csv
replace what="null", with=null
if categories? != null {
  categories = categories.parse_json()
}
if confidence? != null {
  confidence = confidence.float()
}
if score? != null {
  score = score.float()
}
