// Bare `%ASA-...` frames (no syslog envelope) via read_lines + message=line.
// A line that is not an ASA frame must be rejected by the parser.
from_file env("TENZIR_INPUT") {
  read_lines
}
cisco::asa::parse message=line
