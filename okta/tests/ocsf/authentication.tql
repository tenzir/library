from_file f"{env("TENZIR_INPUTS")}/system-log.ndjson" {
  okta::read
}
where eventType == "user.session.start" or eventType == "user.authentication.sso" or eventType == "policy.evaluate_sign_on"
okta::ocsf::map this, this
ocsf_derive
ocsf_cast
sort time
