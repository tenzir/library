// Package-wide constants shared across the Okta OCSF event mappers.

// OCSF `status_id` for Okta `outcome.result` values.
let $status_ids = {
  SUCCESS: 1,
  ALLOW: 1,
  FAILURE: 2,
  DENY: 2,
  CHALLENGE: 99,
}
