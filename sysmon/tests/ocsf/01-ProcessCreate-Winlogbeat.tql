from_file f"{env("TENZIR_INPUTS")}/01-ProcessCreate.winlogbeat.json" {
  read_ndjson
}
sysmon::ocsf::normalize
metadata.log_format = "json"
ocsf_cast

// Ensure deterministic tests.
drop metadata.processed_time
