---
description: Maps LSA groups assigned to a logon to OCSF Authorize Session.
args:
  positional:
    - name: windows
      type: field
      description: The Windows event to map.
    - name: ocsf
      type: field
      description: The field that receives the OCSF event.
---

@name = "ocsf.authorize_session"
$ocsf.category_uid = 3
$ocsf.class_uid = 3003
$ocsf.activity_id = 1
$ocsf.type_uid = $ocsf.class_uid * 100 + $ocsf.activity_id
$ocsf.user = {
  uid: move $windows.EventData.TargetUserSid?,
  name: move $windows.EventData.TargetUserName?,
  domain: move $windows.EventData.TargetDomainName?,
}
$ocsf.session = {uid_alt: move $windows.EventData.TargetLogonId?}
if $windows.EventData.SidList? != null {
  $ocsf.privileges = (move $windows.EventData.SidList).split_regex(r"\s+")
}
