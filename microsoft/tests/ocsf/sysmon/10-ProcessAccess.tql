from_file f"{env("TENZIR_INPUTS")}/sysmon/10-ProcessAccess.xml" {
  read_all
}
microsoft::windows::ocsf::normalize data
metadata.log_format = "xml"
ocsf_cast

// Ensure deterministic tests.
drop metadata.processed_time
