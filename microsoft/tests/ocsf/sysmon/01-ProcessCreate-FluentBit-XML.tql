from_file f"{env("TENZIR_INPUTS")}/sysmon/01-ProcessCreate.xml" {
  read_all
}
this = {
  System: move data,
  Message: "Process Create",
}
microsoft::windows::ocsf::normalize
metadata.log_format = "xml"
ocsf_cast

// Ensure deterministic tests.
drop metadata.processed_time
