---
pre-compare: sort
---

from_file f"{env("TENZIR_INPUTS")}/sysmon/13-RegistryValueSet-real-collector.ndjson" {
  read_ndjson raw=true
}
microsoft::windows::ocsf::normalize
this = {
  event_code: metadata.event_code,
  activity_id: activity_id,
  data: reg_value.data,
}
