---
name: Onboard Check Point logs from NDJSON files
description: |
  Reads already-structured Check Point log records from a file URL, normalizes
  them, and publishes them to the topic `checkpoint`.
---

from "{{ inputs.file-url }}" {
  read_ndjson
}
checkpoint::parse
publish "checkpoint"
