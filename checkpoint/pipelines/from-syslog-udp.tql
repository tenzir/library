---
name: Onboard Check Point syslog via UDP
description: |
  Reads Check Point syslog over UDP, parses the exported firewall fields, and
  publishes normalized records to the topic `checkpoint`.
---

from "{{ inputs.syslog-url }}" {
  read_syslog
}
checkpoint::parse
publish "checkpoint"
