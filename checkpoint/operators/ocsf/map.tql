---
description: Check Point → OCSF
---

if @name == "checkpoint.firewall.detection" {
  checkpoint::ocsf::logs::detection_finding
} else if @name == "checkpoint.firewall.network" {
  checkpoint::ocsf::logs::network_activity
} else {
  checkpoint::ocsf::base
}
