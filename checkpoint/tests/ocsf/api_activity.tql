from_file f"{env("TENZIR_INPUTS")}/checkpoint-expanded.ndjson" {
  read_ndjson
}
checkpoint::parse
where loguid == "api-1"
checkpoint::ocsf::map
ocsf::derive
ocsf::cast
select activity_id,
       actor,
       api,
       class_uid,
       resources,
       src_endpoint,
       status_id,
       unmapped
