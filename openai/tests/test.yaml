# The OCSF mappings call `otel::unroll_logs` and `otel::unroll_traces` from the
# `otel` package. Point the harness at the library root so those operators
# resolve when this package's suite runs on its own.
package-dirs:
  - ../..
