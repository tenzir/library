from_file f"{env("TENZIR_INPUTS")}/../native-span.input" {
  read_ndjson
}
@name = "otel.span"
openai::codex::ocsf
ocsf_derive
ocsf_cast
select metadata.version, metadata.event_code, metadata.profiles, class_name,
       activity_name, ai_agent, api?.operation?, trace, status
