from_file env("TENZIR_INPUT") {
  read_json
}
openai::codex::ocsf_legacy_envelope include_content=true
ocsf_derive
ocsf_cast
where metadata.event_code == "codex.conversation_starts"
select time, metadata.event_code, metadata.profiles, class_name,
       activity_name, severity, privileges, user, session, ai_agent,
       unmapped.mcp_servers
