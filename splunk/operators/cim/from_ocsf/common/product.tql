---
description: Map OCSF product metadata to Splunk CIM vendor_product
---

product = metadata.product.name?
vendor = metadata.product.vendor_name?
cim.vendor_product = product
if vendor != null {
  cim.vendor_product = (vendor + " " + product) if product != null else vendor
}
