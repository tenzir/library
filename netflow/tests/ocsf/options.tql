from_file f"{env("TENZIR_INPUTS")}/threats.ixflow" {
  read_netflow
}
where netflow.record_type == "options"
head 1
this = {source: this, ocsf: {}}
netflow::ocsf::map source, ocsf
this = ocsf
ocsf_cast
