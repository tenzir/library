// Synthetic reader contract, not part of the captured demo corpus.
from_file f"{env("TENZIR_INPUTS")}/native.json" {
  demo::agents::read
}
select schema=@name, event_name=attributes?["event.name"]? else name?
