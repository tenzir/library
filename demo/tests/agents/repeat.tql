every 1ms {
  let $cycle = uuid()
  from {call_id: "call_original"}
  @name = "otel.log"
  demo::agents::rotate_ids seed=$cycle
}
head 2
summarize records=count(), ids=distinct(call_id)
select records, different_cycles=ids.length() == 2
