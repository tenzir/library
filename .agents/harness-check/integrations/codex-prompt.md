Use the project `harness-check` skill to generate nearly all security-relevant
native Codex OpenTelemetry activity now. Run its autonomous workload and real
ephemeral child Codex session without a scope prompt. Never replace a native
event with a mock or lookalike action. Then guide the user through only the
unavoidable approval decisions, exactly one per turn.
