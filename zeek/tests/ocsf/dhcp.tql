from_file f"{env("TENZIR_INPUTS")}/dhcp.log" {
  // Why can't we use read_zeek_json here?
  read_json
  this = this.unflatten(".")
}
_path = "dhcp"
this = {source: this, ocsf: {}}
zeek::ocsf::map source, ocsf
this = ocsf
ocsf_derive
ocsf_cast
