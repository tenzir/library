---
name: Onboard Zeek logs from a URL
description: |
  Loads Zeek logs from a file or URL.
disabled: true
---

let $url = "{{ inputs.url }}"
from_file $url {
  read_zeek_{{ inputs.file-format }}
}
publish "zeek"
