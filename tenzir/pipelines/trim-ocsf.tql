---
name: Trim OCSF events
description: |
  Subscribes to OCSF events, applies size-based trimming, and publishes the
  results to the configured output topic.
disabled: true
---

subscribe "{{ inputs.input-topic }}"
tenzir::trim size="{{ inputs.size }}"
publish "{{ inputs.output-topic }}"
