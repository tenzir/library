---
name: Enrich events with OSINT objects
description: |
  Enriches OCSF network events with OSINT objects and adds the OSINT profile.
disabled: true
---

subscribe "{{ inputs.enrich-input-topic }}"
tenzir::osint::enrich
publish "{{ inputs.enrich-output-topic }}"
