from {
  process: {name: "mimikatz.exe"},
  baseline: null,
  current: frequency_table(["mimikatz.exe"]),
}
strict {
  tenzir::detect::behavior::categorical::score \
    current=current,
    min_samples=1,
    into=behavior
}
select behavior
